/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  trailingSlash: true,
  images: {
    unoptimized: true
  },
  basePath: '/blog-sitem',
  assetPrefix: '/blog-sitem'
}

export default nextConfig
